{
  "name": "season27-frontend",
  "private": true,
  "scripts": {
    "build": "esbuild frontend/app.ts --bundle --minify --outfile=app/static/app.js",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/sortablejs": "^1.15.8",
    "esbuild": "^0.25.0",
    "sortablejs": "^1.15.6",
    "typescript": "^5.7.0"
  }
}
